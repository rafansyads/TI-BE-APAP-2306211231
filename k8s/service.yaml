apiVersion: v1
kind: Service
metadata:
  name: travel-apap-accommodation-be-service
  namespace: default
spec:
  selector:
    app: travel-apap-accommodation-be
  type: ClusterIP
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080